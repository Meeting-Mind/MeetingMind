# AGENT.md

코딩 에이전트 행동 규칙. 속도보다 **정확성·안전**을 우선한다.
- **신중함**은 *문제 이해와 검증*에 적용한다.
- **게으름(최소 코드)**은 *코드 양*에 적용한다.
- 둘이 갈리면 정확성·안전을 만족하는 선택지 중 **가장 작은 변경**을 택한다. 잘못된 위치의 작은 변경은 또 다른 버그다.

## 1. 코딩 전
- 가정을 명시하고, 불확실하면 질문한다. 혼자 판단해 하나를 고르지 않는다.
- 해석이 여러 개면 나열하고, 더 간단한 방법이 있으면 제안한다.
- 먼저 관련 코드를 읽고 실제 흐름을 끝까지 추적한다. 이해 못 한 차이를 넘어가지 않는다.

## 2. 최소 코드 (순서대로 확인)
1. 이 기능이 정말 필요한가? (YAGNI)
2. 기존 헬퍼·유틸·패턴이 있으면 재사용한다.
3. 표준 라이브러리 / 네이티브 기능 / 이미 설치된 의존성으로 되면 그것을 쓴다.
4. 그다음에야 작동하는 최소 코드를 작성한다.

### 구현 판단 순서

새 기능, 새 코드, 새 의존성 추가 전에는 아래 순서로 판단한다.

1. 새 기능이 필요한가?
   - 아니면 구현하지 않는다.
2. 기존 구현이 존재하는가?
   - 있으면 재사용한다.
3. 표준 API, 표준 라이브러리, 네이티브 기능으로 가능한가?
   - 가능하면 그것을 사용한다.
4. 이미 설치된 dependency로 가능한가?
   - 가능하면 그것을 사용한다.
5. 새 라이브러리가 필요한가?
   - 필요하면 이유, 대안, 영향 범위를 문서화한 뒤 추가한다.
6. 위 선택지가 모두 아니면 직접 구현한다.

판단 순서는 건너뛰지 않는다. 새 라이브러리 추가와 직접 구현은 마지막 선택지다.

- 요구되지 않은 추상화·유연성·설정 옵션·보일러플레이트를 넣지 않는다.
- 새 의존성은 피할 수 있으면 추가하지 않는다.
- *논리적으로 불가능한* 경우는 처리하지 않는다. (단, 신뢰 경계의 외부 입력 검증은 반드시 한다.)

## 3. 정밀 수정
- 변경한 모든 줄은 요청과 직접 연결돼야 한다.
- 인접 코드·주석·서식을 임의로 개선하지 않는다. 멀쩡한 코드를 리팩터링하지 않는다.
- 기존 스타일을 따른다.
- 무관한 dead code는 언급만 하고 삭제하지 않는다.
- 본인 수정으로 생긴 orphan(import·변수·함수)만 제거한다.

## 4. 버그 수정
- 증상이 아닌 근본 원인을 고친다.
- 대상 함수의 모든 호출부를 grep해 공유 지점을 한 번만 수정한다. 호출부마다 가드를 넣지 않는다.

## 5. 검증
- 작업을 검증 가능한 성공 기준으로 바꾼다. 다단계면 각 단계에 검증 항목을 붙인다.
- 검증 형태는 복잡도에 맞춘다: 한 줄짜리=불필요 / 버그=재현 검사 / 복잡한 로직=프레임워크 없는 최소 자체 검사 하나(깨지면 실패하는 가장 작은 단위).

## 6. 게으름 금지 항목
문제 이해, 신뢰 경계 입력 검증, 데이터 손실 방지 오류 처리, 보안, 접근성, 명시적으로 요청된 모든 사항.
